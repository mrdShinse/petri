- @title = '質問一覧'
h1 = @title
.table-wrapper
  table.listing
    tr
      th タイトル
      th 内容
      th 投稿者
    - @posts.each do |p|
      tr
        td = link_to p.title, user_post_path(id: p.id)
        td = p.content
        td
          = p.user.family_name
          = p.user.given_name
